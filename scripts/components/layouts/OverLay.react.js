var React = require('react');

// Flux cart view
var OverLay = React.createClass({

  // Render cart view
  render: function() {

    return (
        <div className="overlay"></div>
    );
  }

});

module.exports = OverLay;